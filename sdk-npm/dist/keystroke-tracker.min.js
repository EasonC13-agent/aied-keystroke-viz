(function(root, factory) {
  if (typeof module !== 'undefined' && module.exports) {
    module.exports = factory();
  } else if (typeof define === 'function' && define.amd) {
    define(factory);
  } else {
    root.KeystrokeTracker = factory();
  }
}(typeof self !== 'undefined' ? self : this, function() {
  'use strict';
  // KeystrokeTracker class and helpers
  function KeystrokeTracker() { this.data = []; }
  KeystrokeTracker.prototype.track = function(el) { return this; };
  KeystrokeTracker.prototype.getData = function() { return null; };
  return { KeystrokeTracker: KeystrokeTracker };
}));
