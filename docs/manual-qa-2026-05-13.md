# Manual QA Handoff — 2026-05-13

## Branch
- `main`

## What was patched
1. Renamed ambiguous review filter labels:
   - `Unreviewed` → `Needs Review`
   - added `Not Flagged`
2. Made keystroke-column detection more robust:
   - still accepts `keystroke_log`
   - still accepts `keystroke_data`
   - now also auto-detects renamed columns by inspecting JSON-like payloads
3. Fixed export modal so `keystroke_data` is not offered as a secondary ID column.

## Files changed
- `src/components/Dashboard.tsx`
- `src/components/dashboard/KeystrokeSection.tsx`
- `src/components/dashboard/ExportSection.tsx`
- `public/sample_keystroke_data_renamed.csv` (test fixture)

## What I verified
- `npm run build` passes successfully.
- local dev server starts successfully.

## What still needs manual browser QA
I could not complete host-browser interaction from this environment, so these should be checked manually in the browser.

### A. Review filter labels / behavior
1. Open app.
2. Load `public/sample.csv`.
3. Go to `Review Keystrokes`.
4. Confirm filters visible:
   - `Flagged Only`
   - `Needs Review`
   - `Not Flagged`
   - `All Participants`
5. Confirm behavior:
   - `Flagged Only` shows all flagged participants
   - `Needs Review` shows flagged participants with no verdict yet
   - `Not Flagged` shows only non-flagged participants
   - `All Participants` shows everyone

### B. Renamed keystroke column import
1. Load `public/sample_keystroke_data_renamed.csv`.
2. Confirm import still works even though the column is **not** named `keystroke_log` or `keystroke_data`.
3. Confirm flagged participants and per-question detail render normally.

### C. Export modal secondary ID options
1. Load a file that uses `keystroke_data` as the keystroke field name, or adapt sample data if needed.
2. Open `Finalize & Export` → `Export (All)`.
3. In the secondary ID dropdown, confirm the keystroke payload column is **not** listed as an ID candidate.

### D. Regression check: ID column switching
1. Load `sample.csv`.
2. Review a flagged participant.
3. Set verdict and add a note.
4. Change `Participant ID Column` to `prolific_id` and back.
5. Confirm verdict/note are preserved.

## Suggested commit expectation
If all checks pass, this patch is probably safe to keep on `main`.

## Notes
- This patch improves robustness, but there is still no explicit manual picker for the keystroke column when auto-detection fails. That could be a future enhancement if more Qualtrics variants appear.
